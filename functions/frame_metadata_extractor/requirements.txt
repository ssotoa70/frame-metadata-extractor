# Python dependencies for the frame-metadata-extractor DataEngine function
#
# OpenImageIO Python bindings — primary reader for every supported format
# (EXR, DPX, TIFF, PNG, TGA, HDR/RGBE, JPEG2000, Cineon):
OpenImageIO>=2.2

# S3 access for downloading frame headers from VAST buckets:
boto3>=1.28.0

# VAST DataBase persistence dependencies:
pyarrow>=10.0.0
vastdb>=1.3.9
